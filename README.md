# Snapper plugin for DNF5

This is a native plugin for libdnf5 that brings snapshotting capabilities via Snapper.

The plugin automatically creates snapshots before and after every DNF transaction; no configuration needed.
